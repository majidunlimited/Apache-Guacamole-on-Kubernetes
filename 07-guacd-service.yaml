apiVersion: v1
kind: Service
metadata:
  name: guacd-service
  namespace: default
  labels:
    app: guacd

spec:
  type: ClusterIP

  selector:
    app: guacd

  ports:
    - name: guacd
      port: 4822
      targetPort: 4822
      protocol: TCP
