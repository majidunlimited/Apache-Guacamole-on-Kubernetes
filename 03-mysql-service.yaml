apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: default
  labels:
    app: guacamole-mysql

spec:
  type: ClusterIP

  selector:
    app: guacamole-mysql

  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
